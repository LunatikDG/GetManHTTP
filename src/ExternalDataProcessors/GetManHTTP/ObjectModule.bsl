// strict-types (annotation without at-sign to avoid UsingServiceTag)
// Имена с URI/JSON/JWT намеренно смешивают латиницу и кириллицу.
// BSLLS:LatinAndCyrillicSymbolInWord-off

#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Создает структуру временного хранилища для обработки
//
// Возвращаемое значение:
//	Структура:
//		* ИспользованиеРазрешено - Булево - флаг разрешающий использовать обработку
//
Функция СоздатьСтруктуру() Экспорт
	
	СтруктураХранилища = Новый Структура();
	
	СтруктураХранилища.Вставить("ИспользованиеРазрешено", Истина);
	
	Возврат СтруктураХранилища;
	
КонецФункции

#Область Авторизация

// Максимум слотов полей авторизации на формах обработки.
//
// Возвращаемое значение:
//  Число - максимальное количество полей авторизации на форме.
//
Функция МаксимумПолейАвторизацииНаФорме() Экспорт
	
	Возврат 6;
	
КонецФункции

Функция ИдентификаторТипаАвторизацииТокен() Экспорт
	
	Возврат "Token";
	
КонецФункции

Функция ИдентификаторТипаАвторизацииBearerToken() Экспорт
	
	Возврат "BearerToken";
	
КонецФункции

Функция ИдентификаторТипаАвторизацииApiKey() Экспорт
	
	Возврат "ApiKey";
	
КонецФункции

Функция ИдентификаторТипаАвторизацииJwtBearer() Экспорт
	
	Возврат "JwtBearer";
	
КонецФункции

Функция ИдентификаторТипаАвторизацииБезАвторизации() Экспорт
	
	Возврат "NoAuth";
	
КонецФункции

Функция ИдентификаторТипаАвторизацииНаследоватьОтВладельца() Экспорт
	
	Возврат "InheritFromOwner";
	
КонецФункции

Функция ИдентификаторТипаАвторизацииBasicAuth() Экспорт
	
	Возврат "BasicAuth";
	
КонецФункции

Функция ЭтоТипАвторизацииНаследоватьОтВладельца(Знач ТипАвторизации) Экспорт
	
	Возврат ТипАвторизации = ИдентификаторТипаАвторизацииНаследоватьОтВладельца()
		Или ПустаяСтрока(ТипАвторизации);
	
КонецФункции

Функция ЭтоПоддерживаемыйТипАвторизации(Знач ТипАвторизации) Экспорт
	
	Возврат ТипАвторизации = ИдентификаторТипаАвторизацииBasicAuth()
		Или ТипАвторизации = ИдентификаторТипаАвторизацииТокен()
		Или ТипАвторизации = ИдентификаторТипаАвторизацииBearerToken()
		Или ТипАвторизации = ИдентификаторТипаАвторизацииApiKey()
		Или ТипАвторизации = ИдентификаторТипаАвторизацииJwtBearer()
		Или ТипАвторизации = ИдентификаторТипаАвторизацииБезАвторизации()
		Или ТипАвторизации = ИдентификаторТипаАвторизацииНаследоватьОтВладельца();
	
КонецФункции

Функция ПрефиксТокенаАвторизацииПоУмолчанию() Экспорт
	
	Возврат ИдентификаторТипаАвторизацииТокен();
	
КонецФункции

Функция ПрефиксBearerАвторизации() Экспорт
	
	Возврат "Bearer";
	
КонецФункции

Функция ИмяЗаголовкаAuthorization() Экспорт
	
	Возврат "Authorization";
	
КонецФункции

Функция КлючПоляАвторизацииПрефиксТокена() Экспорт
	
	Возврат "tokenPrefix";
	
КонецФункции

Функция КлючПоляАвторизацииТокен() Экспорт
	
	Возврат "token";
	
КонецФункции

Функция КлючПоляАвторизацииApiKeyИмя() Экспорт
	
	Возврат "key";
	
КонецФункции

Функция КлючПоляАвторизацииApiKeyЗначение() Экспорт
	
	Возврат "value";
	
КонецФункции

Функция КлючПоляАвторизацииApiKeyДобавитьВ() Экспорт
	
	Возврат "addTo";
	
КонецФункции

Функция КлючПоляАвторизацииJwtАлгоритм() Экспорт
	
	Возврат "algorithm";
	
КонецФункции

Функция КлючПоляАвторизацииJwtСекрет() Экспорт
	
	Возврат "secret";
	
КонецФункции

Функция КлючПоляАвторизацииJwtClaims() Экспорт
	
	Возврат "claims";
	
КонецФункции

Функция КлючПоляАвторизацииJwtTTL() Экспорт
	
	Возврат "ttlSeconds";
	
КонецФункции

Функция РежимДобавленияApiKeyВЗаголовок() Экспорт
	
	Возврат "Header";
	
КонецФункции

Функция РежимДобавленияApiKeyВQuery() Экспорт
	
	Возврат "Query";
	
КонецФункции

Функция ИмяЗаголовкаApiKeyПоУмолчанию() Экспорт
	
	Возврат "X-API-Key";
	
КонецФункции

Функция АлгоритмJwtHS256() Экспорт
	
	Возврат "HS256";
	
КонецФункции

Функция АлгоритмJwtHS512() Экспорт
	
	Возврат "HS512";
	
КонецФункции

Функция ВидПравилаАвторизацииComputedHeader() Экспорт
	
	Возврат "ComputedHeader";
	
КонецФункции

Функция ОбработчикComputedHeaderJwtBearer() Экспорт
	
	Возврат ИдентификаторТипаАвторизацииJwtBearer();
	
КонецФункции

// Возвращает схему полей и правил для типа авторизации.
//
// Параметры:
//  ТипАвторизации - Строка - идентификатор типа (BasicAuth, Token, BearerToken, ApiKey, JwtBearer, …).
//
// Возвращаемое значение:
//  Структура - Поля (Массив), Правила (Массив).
//
Функция ОписаниеСхемыАвторизации(Знач ТипАвторизации) Экспорт
	
	Схема = Новый Структура;
	Схема.Вставить("Поля", Новый Массив);
	Схема.Вставить("Правила", Новый Массив);
	
	Если ТипАвторизации = ИдентификаторТипаАвторизацииBasicAuth() Тогда
		
		ПолеЛогина = НовоеПолеСхемыАвторизации("login", "Логин", Ложь, Ложь, "");
		ПолеПароля = НовоеПолеСхемыАвторизации("password", "Пароль", Истина, Ложь, "");
		ПравилоBasic = НовоеПравилоАвторизацииБазовогоСоединения("login", "password");
		
		Схема.Поля.Добавить(ПолеЛогина);
		Схема.Поля.Добавить(ПолеПароля);
		Схема.Правила.Добавить(ПравилоBasic);
		
	ИначеЕсли ТипАвторизации = ИдентификаторТипаАвторизацииТокен() Тогда
		
		ПолеПрефикса = НовоеПолеСхемыАвторизации(
			КлючПоляАвторизацииПрефиксТокена(),
			"Префикс токена",
			Ложь,
			Ложь,
			ПрефиксТокенаАвторизацииПоУмолчанию());
		ПолеТокена = НовоеПолеСхемыАвторизации(
			КлючПоляАвторизацииТокен(),
			"Токен",
			Истина,
			Ложь,
			"");
		ШаблонЗаголовка = "{" + КлючПоляАвторизацииПрефиксТокена() + "} {"
			+ КлючПоляАвторизацииТокен() + "}";
		ПравилоЗаголовка = НовоеПравилоАвторизацииЗаголовка(ИмяЗаголовкаAuthorization(), ШаблонЗаголовка);
		
		Схема.Поля.Добавить(ПолеПрефикса);
		Схема.Поля.Добавить(ПолеТокена);
		Схема.Правила.Добавить(ПравилоЗаголовка);
		
	ИначеЕсли ТипАвторизации = ИдентификаторТипаАвторизацииBearerToken() Тогда
		
		ПолеТокена = НовоеПолеСхемыАвторизации(
			КлючПоляАвторизацииТокен(),
			"Токен",
			Истина,
			Ложь,
			"");
		ШаблонЗаголовка = ПрефиксBearerАвторизации() + " {" + КлючПоляАвторизацииТокен() + "}";
		ПравилоЗаголовка = НовоеПравилоАвторизацииЗаголовка(ИмяЗаголовкаAuthorization(), ШаблонЗаголовка);
		
		Схема.Поля.Добавить(ПолеТокена);
		Схема.Правила.Добавить(ПравилоЗаголовка);
		
	ИначеЕсли ТипАвторизации = ИдентификаторТипаАвторизацииApiKey() Тогда
		
		ПолеИмени = НовоеПолеСхемыАвторизации(
			КлючПоляАвторизацииApiKeyИмя(),
			"Имя ключа",
			Ложь,
			Ложь,
			ИмяЗаголовкаApiKeyПоУмолчанию());
		ПолеЗначения = НовоеПолеСхемыАвторизации(
			КлючПоляАвторизацииApiKeyЗначение(),
			"Значение",
			Истина,
			Ложь,
			"");
		ПолеРежима = НовоеПолеСхемыАвторизации(
			КлючПоляАвторизацииApiKeyДобавитьВ(),
			"Добавить в",
			Ложь,
			Ложь,
			РежимДобавленияApiKeyВЗаголовок(),
			ВариантыВыбораРежимаApiKey());
		ПравилоApiKey = НовоеПравилоАвторизацииHeaderOrQuery(
			КлючПоляАвторизацииApiKeyИмя(),
			КлючПоляАвторизацииApiKeyЗначение(),
			КлючПоляАвторизацииApiKeyДобавитьВ());
		
		Схема.Поля.Добавить(ПолеИмени);
		Схема.Поля.Добавить(ПолеЗначения);
		Схема.Поля.Добавить(ПолеРежима);
		Схема.Правила.Добавить(ПравилоApiKey);
		
	ИначеЕсли ТипАвторизации = ИдентификаторТипаАвторизацииJwtBearer() Тогда
		
		ПолеАлгоритма = НовоеПолеСхемыАвторизации(
			КлючПоляАвторизацииJwtАлгоритм(),
			"Алгоритм",
			Ложь,
			Ложь,
			АлгоритмJwtHS256(),
			ВариантыВыбораАлгоритмаJwt());
		ПолеСекрета = НовоеПолеСхемыАвторизации(
			КлючПоляАвторизацииJwtСекрет(),
			"Секрет",
			Истина,
			Ложь,
			"");
		ПолеClaims = НовоеПолеСхемыАвторизации(
			КлючПоляАвторизацииJwtClaims(),
			"Claims (JSON)",
			Ложь,
			Истина,
			"{}");
		ПолеTTL = НовоеПолеСхемыАвторизации(
			КлючПоляАвторизацииJwtTTL(),
			"TTL (сек)",
			Ложь,
			Ложь,
			"3600");
		ПравилоJwt = НовоеПравилоАвторизацииComputedHeader(
			ИмяЗаголовкаAuthorization(),
			ОбработчикComputedHeaderJwtBearer());
		
		Схема.Поля.Добавить(ПолеАлгоритма);
		Схема.Поля.Добавить(ПолеСекрета);
		Схема.Поля.Добавить(ПолеClaims);
		Схема.Поля.Добавить(ПолеTTL);
		Схема.Правила.Добавить(ПравилоJwt);
		
	Иначе
		Возврат Схема;
	КонецЕсли;
	
	Возврат Схема;
	
КонецФункции

Процедура ПерестроитьПараметрыАвторизацииПоСхеме(
	ТаблицаПараметров,
	Знач ТипАвторизации,
	Знач СохранятьЗначения = Истина) Экспорт
	
	Схема = ОписаниеСхемыАвторизации(ТипАвторизации);
	СтарыеЗначения = Новый Соответствие;
	
	Если СохранятьЗначения Тогда
		Для Каждого СтрокаПараметра Из ТаблицаПараметров Цикл
			СтарыеЗначения.Вставить(СтрокаПараметра.Ключ, СтрокаПараметра.Значение);
		КонецЦикла;
	КонецЕсли;
	
	ТаблицаПараметров.Очистить();
	
	Для Каждого ОписаниеПоля Из Схема.Поля Цикл
		
		ЗначениеПоля = Неопределено;
		Если СохранятьЗначения Тогда
			ЗначениеПоля = СтарыеЗначения.Получить(ОписаниеПоля.Ключ);
		КонецЕсли;
		Если ЗначениеПоля = Неопределено Тогда
			ЗначениеПоля = ОписаниеПоля.ЗначениеПоУмолчанию;
		КонецЕсли;
		
		УстановитьЗначениеПараметраАвторизации(
			ТаблицаПараметров,
			ОписаниеПоля.Ключ,
			ЗначениеПоля,
			ОписаниеПоля.Секретный);
		
	КонецЦикла;
	
КонецПроцедуры

Функция СоответствиеЗначенийПараметровАвторизации(Знач ТаблицаПараметров) Экспорт
	
	Результат = Новый Соответствие;
	
	Для Каждого СтрокаПараметра Из ТаблицаПараметров Цикл
		Результат.Вставить(СтрокаПараметра.Ключ, СтрокаПараметра.Значение);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура УстановитьЗначениеПараметраАвторизации(
	ТаблицаПараметров,
	Знач Ключ,
	Знач Значение,
	Знач Секретный) Экспорт
	
	Для Каждого СтрокаПараметра Из ТаблицаПараметров Цикл
		
		Если СтрокаПараметра.Ключ = Ключ Тогда
			СтрокаПараметра.Значение = Значение;
			СтрокаПараметра.Секретный = Секретный;
			Возврат;
		КонецЕсли;
		
	КонецЦикла;
	
	НоваяСтрока = ТаблицаПараметров.Добавить();
	НоваяСтрока.Ключ = Ключ;
	НоваяСтрока.Значение = Значение;
	НоваяСтрока.Секретный = Секретный;
	
КонецПроцедуры

Функция ЗначениеПараметраАвторизацииИзСоответствия(
	Знач ЗначенияПараметров,
	Знач Ключ,
	Знач ЗначениеПоУмолчанию = "") Экспорт
	
	Значение = ЗначенияПараметров.Получить(Ключ);
	Если Значение = Неопределено Тогда
		Возврат ЗначениеПоУмолчанию;
	КонецЕсли;
	
	Возврат Значение;
	
КонецФункции

Процедура ПрименитьПравилаАвторизацииКСтруктуреURI(
	URI,
	Знач ТипАвторизации,
	Знач ЗначенияПараметров) Экспорт
	
	Схема = ОписаниеСхемыАвторизации(ТипАвторизации);
	
	Для Каждого Правило Из Схема.Правила Цикл
		
		Если Правило.Вид = "ConnectionBasic" Тогда
			URI.Логин = ЗначениеПараметраАвторизацииИзСоответствия(ЗначенияПараметров, Правило.КлючЛогина);
			URI.Пароль = ЗначениеПараметраАвторизацииИзСоответствия(ЗначенияПараметров, Правило.КлючПароля);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ПрименитьПравилаАвторизацииКЗаголовкам(
	Заголовки,
	Знач ТипАвторизации,
	Знач ЗначенияПараметров) Экспорт
	
	Схема = ОписаниеСхемыАвторизации(ТипАвторизации);
	
	Для Каждого Правило Из Схема.Правила Цикл
		
		Если Правило.Вид = "Header" Тогда
			
			ЗначениеЗаголовка = ПодставитьПараметрыВШаблонАвторизации(Правило.Шаблон, ЗначенияПараметров);
			Заголовки.Вставить(Правило.Имя, ЗначениеЗаголовка);
			
		КонецЕсли;
		
		Если Правило.Вид = "HeaderOrQuery" Тогда
			ПрименитьПравилоHeaderOrQueryКЗаголовкам(Заголовки, Правило, ЗначенияПараметров);
		КонецЕсли;
		
		Если Правило.Вид = ВидПравилаАвторизацииComputedHeader() Тогда
			
			ЗначениеЗаголовка = ВычислитьЗначениеComputedHeaderАвторизации(
				Правило.Обработчик,
				ЗначенияПараметров);
			Если ЗначениеЗаголовка <> Неопределено Тогда
				Заголовки.Вставить(Правило.Имя, ЗначениеЗаголовка);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Функция СтрокаПараметровАвторизацииЗапроса(Знач ТипАвторизации, Знач ЗначенияПараметров) Экспорт
	
	Схема = ОписаниеСхемыАвторизации(ТипАвторизации);
	Части = Новый Массив;
	
	Для Каждого Правило Из Схема.Правила Цикл
		
		Если Правило.Вид = "Query" Тогда
			
			ЗначениеПараметра = ПодставитьПараметрыВШаблонАвторизации(Правило.Шаблон, ЗначенияПараметров);
			Части.Добавить(Правило.Имя + "=" + ЗначениеПараметра);
			
		КонецЕсли;
		
		Если Правило.Вид = "HeaderOrQuery" Тогда
			ДобавитьПараметрHeaderOrQueryВМассив(Части, Правило, ЗначенияПараметров);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат СтрСоединить(Части, "&");
	
КонецФункции

// Собирает JWT (header.payload.signature) по параметрам схемы JwtBearer.
//
// Параметры:
//  ЗначенияПараметров - Соответствие - значения полей схемы (algorithm, secret, claims, ttlSeconds).
//
// Возвращаемое значение:
//  Строка - компактный JWT или пустая строка при отсутствии данных для подписи.
//
Функция СобратьJwtBearerТокен(Знач ЗначенияПараметров) Экспорт
	
	КлючАлгоритма = КлючПоляАвторизацииJwtАлгоритм();
	АлгоритмПоУмолчанию = АлгоритмJwtHS256();
	АлгоритмИзПараметров = ЗначениеПараметраАвторизацииИзСоответствия(
		ЗначенияПараметров,
		КлючАлгоритма,
		АлгоритмПоУмолчанию);
	Алгоритм = НормализованныйАлгоритмJwt(АлгоритмИзПараметров);
	
	КлючСекрета = КлючПоляАвторизацииJwtСекрет();
	СекретИзПараметров = ЗначениеПараметраАвторизацииИзСоответствия(
		ЗначенияПараметров,
		КлючСекрета,
		"");
	Секрет = Строка(СекретИзПараметров);
	
	КлючClaims = КлючПоляАвторизацииJwtClaims();
	ClaimsИзПараметров = ЗначениеПараметраАвторизацииИзСоответствия(
		ЗначенияПараметров,
		КлючClaims,
		"{}");
	ClaimsТекст = Строка(ClaimsИзПараметров);
	
	КлючTTL = КлючПоляАвторизацииJwtTTL();
	TTLИзПараметров = ЗначениеПараметраАвторизацииИзСоответствия(
		ЗначенияПараметров,
		КлючTTL,
		"0");
	TTLСтрока = Строка(TTLИзПараметров);
	
	Payload = ПрочитатьClaimsJwtИзJSON(ClaimsТекст);
	ПрименитьTTLКClaimsJwt(Payload, TTLСтрока);
	
	ЗаголовокJwt = Новый Структура;
	ЗаголовокJwt.Вставить("alg", Алгоритм);
	ЗаголовокJwt.Вставить("typ", "JWT");
	
	ЧастьЗаголовка = Base64URLИзСтрокиUTF8(ЗаписатьЗначениеВJSONСтрокуАвторизации(ЗаголовокJwt));
	ЧастьPayload = Base64URLИзСтрокиUTF8(ЗаписатьЗначениеВJSONСтрокуАвторизации(Payload));
	ПодписываемыеДанные = ЧастьЗаголовка + "." + ЧастьPayload;
	
	Подпись = HMACJwt(Секрет, ПодписываемыеДанные, Алгоритм);
	
	Возврат ПодписываемыеДанные + "." + Base64URLИзДвоичныхДанных(Подпись);
	
КонецФункции

Функция НормализованныйАлгоритмJwt(Знач Алгоритм) Экспорт
	
	АлгоритмНорм = ВРег(СокрЛП(Строка(Алгоритм)));
	Если АлгоритмНорм = АлгоритмJwtHS512() Тогда
		Возврат АлгоритмJwtHS512();
	КонецЕсли;
	
	Возврат АлгоритмJwtHS256();
	
КонецФункции

Функция НормализованныйРежимДобавленияApiKey(Знач Режим) Экспорт
	
	РежимНорм = ВРег(СокрЛП(Строка(Режим)));
	Если РежимНорм = ВРег(РежимДобавленияApiKeyВQuery())
		Или РежимНорм = "QUERY" Тогда
		Возврат РежимДобавленияApiKeyВQuery();
	КонецЕсли;
	
	Возврат РежимДобавленияApiKeyВЗаголовок();
	
КонецФункции

#КонецОбласти

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#Область АвторизацияСлужебные

Процедура ПрименитьПравилоHeaderOrQueryКЗаголовкам(Заголовки, Знач Правило, Знач ЗначенияПараметров)
	
	РежимИзПараметров = ЗначениеПараметраАвторизацииИзСоответствия(
		ЗначенияПараметров,
		Правило.КлючРежима);
	Режим = НормализованныйРежимДобавленияApiKey(РежимИзПараметров);
	Если Режим <> РежимДобавленияApiKeyВЗаголовок() Тогда
		Возврат;
	КонецЕсли;
	
	ИмяИзПараметров = ЗначениеПараметраАвторизацииИзСоответствия(
		ЗначенияПараметров,
		Правило.КлючИмени);
	ИмяЗаголовка = СокрЛП(Строка(ИмяИзПараметров));
	Если ПустаяСтрока(ИмяЗаголовка) Тогда
		Возврат;
	КонецЕсли;
	
	ЗначениеИзПараметров = ЗначениеПараметраАвторизацииИзСоответствия(
		ЗначенияПараметров,
		Правило.КлючЗначения);
	ЗначениеЗаголовка = Строка(ЗначениеИзПараметров);
	Заголовки.Вставить(ИмяЗаголовка, ЗначениеЗаголовка);
	
КонецПроцедуры

Процедура ДобавитьПараметрHeaderOrQueryВМассив(Части, Знач Правило, Знач ЗначенияПараметров)
	
	РежимИзПараметров = ЗначениеПараметраАвторизацииИзСоответствия(
		ЗначенияПараметров,
		Правило.КлючРежима);
	Режим = НормализованныйРежимДобавленияApiKey(РежимИзПараметров);
	Если Режим <> РежимДобавленияApiKeyВQuery() Тогда
		Возврат;
	КонецЕсли;
	
	ИмяИзПараметров = ЗначениеПараметраАвторизацииИзСоответствия(
		ЗначенияПараметров,
		Правило.КлючИмени);
	ИмяПараметра = СокрЛП(Строка(ИмяИзПараметров));
	Если ПустаяСтрока(ИмяПараметра) Тогда
		Возврат;
	КонецЕсли;
	
	ЗначениеИзПараметров = ЗначениеПараметраАвторизацииИзСоответствия(
		ЗначенияПараметров,
		Правило.КлючЗначения);
	ЗначениеПараметраСтрокой = Строка(ЗначениеИзПараметров);
	Части.Добавить(ИмяПараметра + "=" + ЗначениеПараметраСтрокой);
	
КонецПроцедуры

Функция ВариантыВыбораРежимаApiKey()
	
	Результат = Новый Массив;
	РежимЗаголовок = РежимДобавленияApiKeyВЗаголовок();
	РежимQuery = РежимДобавленияApiKeyВQuery();
	ВариантЗаголовок = НовыйВариантВыбораПоляАвторизации(РежимЗаголовок, "Заголовок");
	ВариантQuery = НовыйВариантВыбораПоляАвторизации(РежимQuery, "Query-параметр");
	Результат.Добавить(ВариантЗаголовок);
	Результат.Добавить(ВариантQuery);
	Возврат Результат;
	
КонецФункции

Функция ВариантыВыбораАлгоритмаJwt()
	
	Результат = Новый Массив;
	АлгоритмHS256 = АлгоритмJwtHS256();
	АлгоритмHS512 = АлгоритмJwtHS512();
	ВариантHS256 = НовыйВариантВыбораПоляАвторизации(АлгоритмHS256, АлгоритмHS256);
	ВариантHS512 = НовыйВариантВыбораПоляАвторизации(АлгоритмHS512, АлгоритмHS512);
	Результат.Добавить(ВариантHS256);
	Результат.Добавить(ВариантHS512);
	Возврат Результат;
	
КонецФункции

Функция НовыйВариантВыбораПоляАвторизации(Знач Значение, Знач Представление)
	
	Вариант = Новый Структура;
	Вариант.Вставить("Значение", Значение);
	Вариант.Вставить("Представление", Представление);
	Возврат Вариант;
	
КонецФункции

Функция НовоеПолеСхемыАвторизации(
	Знач Ключ,
	Знач Заголовок,
	Знач Секретный,
	Знач Многострочный,
	Знач ЗначениеПоУмолчанию,
	Знач ВариантыВыбора = Неопределено)
	
	Поле = Новый Структура;
	Поле.Вставить("Ключ", Ключ);
	Поле.Вставить("Заголовок", Заголовок);
	Поле.Вставить("Секретный", Секретный);
	Поле.Вставить("Многострочный", Многострочный);
	Поле.Вставить("ЗначениеПоУмолчанию", ЗначениеПоУмолчанию);
	Если ВариантыВыбора = Неопределено Тогда
		Поле.Вставить("ВариантыВыбора", Новый Массив);
	Иначе
		Поле.Вставить("ВариантыВыбора", ВариантыВыбора);
	КонецЕсли;
	
	Возврат Поле;
	
КонецФункции

Функция НовоеПравилоАвторизацииБазовогоСоединения(Знач КлючЛогина, Знач КлючПароля)
	
	Правило = Новый Структура;
	Правило.Вставить("Вид", "ConnectionBasic");
	Правило.Вставить("КлючЛогина", КлючЛогина);
	Правило.Вставить("КлючПароля", КлючПароля);
	
	Возврат Правило;
	
КонецФункции

Функция НовоеПравилоАвторизацииЗаголовка(Знач ИмяЗаголовка, Знач ШаблонЗначения)
	
	Правило = Новый Структура;
	Правило.Вставить("Вид", "Header");
	Правило.Вставить("Имя", ИмяЗаголовка);
	Правило.Вставить("Шаблон", ШаблонЗначения);
	
	Возврат Правило;
	
КонецФункции

Функция НовоеПравилоАвторизацииHeaderOrQuery(Знач КлючИмени, Знач КлючЗначения, Знач КлючРежима)
	
	Правило = Новый Структура;
	Правило.Вставить("Вид", "HeaderOrQuery");
	Правило.Вставить("КлючИмени", КлючИмени);
	Правило.Вставить("КлючЗначения", КлючЗначения);
	Правило.Вставить("КлючРежима", КлючРежима);
	
	Возврат Правило;
	
КонецФункции

Функция НовоеПравилоАвторизацииComputedHeader(Знач ИмяЗаголовка, Знач Обработчик)
	
	Правило = Новый Структура;
	Правило.Вставить("Вид", ВидПравилаАвторизацииComputedHeader());
	Правило.Вставить("Имя", ИмяЗаголовка);
	Правило.Вставить("Обработчик", Обработчик);
	
	Возврат Правило;
	
КонецФункции

Функция ПодставитьПараметрыВШаблонАвторизации(Знач Шаблон, Знач ЗначенияПараметров)
	
	Результат = Строка(Шаблон);
	
	Для Каждого КлючИЗначение Из ЗначенияПараметров Цикл
		Результат = СтрЗаменить(Результат, "{" + КлючИЗначение.Ключ + "}", Строка(КлючИЗначение.Значение));
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ВычислитьЗначениеComputedHeaderАвторизации(Знач Обработчик, Знач ЗначенияПараметров)
	
	Если Обработчик = ОбработчикComputedHeaderJwtBearer() Тогда
		Токен = СобратьJwtBearerТокен(ЗначенияПараметров);
		Если ПустаяСтрока(Токен) Тогда
			Возврат Неопределено;
		КонецЕсли;
		Возврат ПрефиксBearerАвторизации() + " " + Токен;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

Функция ПрочитатьClaimsJwtИзJSON(Знач ТекстJSON)
	
	Результат = Новый Соответствие;
	Текст = СокрЛП(Строка(ТекстJSON));
	Если ПустаяСтрока(Текст) Тогда
		Возврат Результат;
	КонецЕсли;
	
	Попытка
		Чтение = Новый ЧтениеJSON;
		Чтение.УстановитьСтроку(Текст);
		Значение = ПрочитатьJSON(Чтение, Истина);
		Чтение.Закрыть();
	Исключение
		ВызватьИсключение "Некорректный JSON в Claims JWT: "
			+ ОбработкаОшибок.КраткоеПредставлениеОшибки(ИнформацияОбОшибке());
	КонецПопытки;
	
	Если ТипЗнч(Значение) = Тип("Соответствие") Тогда
		Возврат Значение;
	ИначеЕсли ТипЗнч(Значение) = Тип("Структура") Тогда
		Для Каждого КлючИЗначение Из Значение Цикл
			Результат.Вставить(КлючИЗначение.Ключ, КлючИЗначение.Значение);
		КонецЦикла;
		Возврат Результат;
	Иначе
		ВызватьИсключение "Claims JWT должны быть JSON-объектом";
	КонецЕсли;
	
КонецФункции

Процедура ПрименитьTTLКClaimsJwt(Payload, Знач TTLСтрока)
	
	ОписаниеЧисла = Новый ОписаниеТипов("Число", , , Новый КвалификаторыЧисла(РазрядностьЦелогоTTL(), 0));
	TTLЧисло = ОписаниеЧисла.ПривестиЗначение(СокрЛП(Строка(TTLСтрока)));
	
	Если TTLЧисло <= 0 Тогда
		Возврат;
	КонецЕсли;
	
	Сейчас = ТекущийUnixTimestamp();
	ВремяИстечения = Сейчас + Цел(TTLЧисло);
	Payload.Вставить("iat", Сейчас);
	Payload.Вставить("exp", ВремяИстечения);
	
КонецПроцедуры

Функция ТекущийUnixTimestamp()
	
	Возврат Цел(ТекущаяУниверсальнаяДата() - Дата(1970, 1, 1, 0, 0, 0));
	
КонецФункции

Функция ЗаписатьЗначениеВJSONСтрокуАвторизации(Знач Значение)
	
	Запись = Новый ЗаписьJSON;
	Запись.УстановитьСтроку(Новый ПараметрыЗаписиJSON(ПереносСтрокJSON.Нет));
	ЗаписатьJSON(Запись, Значение);
	Возврат Запись.Закрыть();
	
КонецФункции

Функция Base64URLИзСтрокиUTF8(Знач СтрокаДанных)
	
	Возврат Base64URLИзДвоичныхДанных(ПолучитьДвоичныеДанныеИзСтроки(СтрокаДанных, КодировкаТекста.UTF8));
	
КонецФункции

Функция Base64URLИзДвоичныхДанных(Знач ДвоичныеДанные)
	
	Результат = Base64Строка(ДвоичныеДанные);
	Результат = СтрЗаменить(Результат, Символы.ПС, "");
	Результат = СтрЗаменить(Результат, Символы.ВК, "");
	Результат = СтрЗаменить(Результат, "+", "-");
	Результат = СтрЗаменить(Результат, "/", "_");
	
	Пока Прав(Результат, 1) = "=" Цикл
		Результат = Лев(Результат, СтрДлина(Результат) - 1);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция HMACJwt(Знач Секрет, Знач Данные, Знач Алгоритм)
	
	Если Алгоритм = АлгоритмJwtHS512() Тогда
		Возврат HMACПоХешФункции(Секрет, Данные, ХешФункция.SHA512, РазмерБлокаHmacSha512());
	КонецЕсли;
	
	Возврат HMACПоХешФункции(Секрет, Данные, ХешФункция.SHA256, РазмерБлокаHmacSha256());
	
КонецФункции

// HMAC (RFC 2104) поверх встроенного ХешированиеДанных.
//
// Параметры:
//  КлючСтрока - Строка - секретный ключ.
//  ДанныеСтрока - Строка - подписываемые данные.
//  ТипХеша - ХешФункция - SHA256 или SHA512.
//  РазмерБлока - Число - размер блока HMAC в байтах (64 для SHA256, 128 для SHA512).
//
// Возвращаемое значение:
//  ДвоичныеДанные - код аутентификации сообщения.
//
Функция HMACПоХешФункции(Знач КлючСтрока, Знач ДанныеСтрока, Знач ТипХеша, Знач РазмерБлока)
	
	Ключ = ПолучитьДвоичныеДанныеИзСтроки(Строка(КлючСтрока), КодировкаТекста.UTF8);
	Данные = ПолучитьДвоичныеДанныеИзСтроки(Строка(ДанныеСтрока), КодировкаТекста.UTF8);
	
	Если Ключ.Размер() > РазмерБлока Тогда
		Ключ = ХешСуммаДвоичныхДанных(Ключ, ТипХеша);
	КонецЕсли;
	
	КлючHex = ПолучитьHexСтрокуИзДвоичныхДанных(Ключ);
	Если Ключ.Размер() < РазмерБлока Тогда
		КлючHex = Лев(КлючHex + ПовторитьHexБайт("00", РазмерБлока), РазмерБлока * ДлинаHexСимволовНаБайт());
	КонецЕсли;
	КлючБуфер = ПолучитьБуферДвоичныхДанныхИзHexСтроки(КлючHex);
	
	Ipad = ПолучитьБуферДвоичныхДанныхИзHexСтроки(ПовторитьHexБайт("36", РазмерБлока));
	Opad = ПолучитьБуферДвоичныхДанныхИзHexСтроки(ПовторитьHexБайт("5c", РазмерБлока));
	Ipad.ЗаписатьПобитовоеИсключительноеИли(0, КлючБуфер);
	Opad.ЗаписатьПобитовоеИсключительноеИли(0, КлючБуфер);
	
	ВнутренниеДанные = СоединитьДвоичныеДанныеИзБуферов(
		Ipad,
		ПолучитьБуферДвоичныхДанныхИзДвоичныхДанных(Данные));
	ВнутреннийХеш = ХешСуммаДвоичныхДанных(ВнутренниеДанные, ТипХеша);
	ВнешниеДанные = СоединитьДвоичныеДанныеИзБуферов(
		Opad,
		ПолучитьБуферДвоичныхДанныхИзДвоичныхДанных(ВнутреннийХеш));
	
	Возврат ХешСуммаДвоичныхДанных(ВнешниеДанные, ТипХеша);
	
КонецФункции

Функция ХешСуммаДвоичныхДанных(Знач ДвоичныеДанные, Знач ТипХеша)
	
	Хеширование = Новый ХешированиеДанных(ТипХеша);
	Хеширование.Добавить(ДвоичныеДанные);
	Возврат Хеширование.ХешСумма;
	
КонецФункции

Функция ПовторитьHexБайт(Знач HexБайт, Знач Количество)
	
	Результат = "";
	ДлинаЦели = Количество * ДлинаHexСимволовНаБайт();
	Пока СтрДлина(Результат) < ДлинаЦели Цикл
		Результат = Результат + HexБайт;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция СоединитьДвоичныеДанныеИзБуферов(Знач Буфер1, Знач Буфер2)
	
	Данные1 = ПолучитьДвоичныеДанныеИзБуфераДвоичныхДанных(Буфер1);
	Данные2 = ПолучитьДвоичныеДанныеИзБуфераДвоичныхДанных(Буфер2);
	Части = Новый Массив;
	Части.Добавить(Данные1);
	Части.Добавить(Данные2);
	Возврат СоединитьДвоичныеДанные(Части);
	
КонецФункции

Функция РазмерБлокаHmacSha256()
	
	Возврат 64;
	
КонецФункции

Функция РазмерБлокаHmacSha512()
	
	Возврат 128;
	
КонецФункции

Функция ДлинаHexСимволовНаБайт()
	
	Возврат 2;
	
КонецФункции

Функция РазрядностьЦелогоTTL()
	
	Возврат 10;
	
КонецФункции

#КонецОбласти

#КонецОбласти

#КонецЕсли

// BSLLS:LatinAndCyrillicSymbolInWord-on
